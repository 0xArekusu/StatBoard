# Release Notes — v1.1.1

## Release Utilisateur

- **Écran de démarrage amélioré** — Le splash screen prend maintenant en charge le mode sombre
- **Correction de la mise à jour forcée (iOS)** — Le bouton de mise à jour redirige correctement vers l'App Store
- **Correction de la légende des statistiques** — La légende du tableau de stats est maintenant scrollable et ne déborde plus de l'écran
- **Correction d'alignement du tableau de stats** — Les colonnes du tableau de statistiques joueurs sont mieux alignées

---

## Release Dev

**Scope** : Fix patch — splash screen (dark mode), redirection App Store (force update iOS), correctifs UI tableau de stats

### Splash screen (`64b90fd`)
- `64b90fd` — Migration du `splash` legacy vers le plugin `expo-splash-screen`, ajout d'une variante dark mode

### Force update / App Store (`e1d5f9a`)
- `e1d5f9a` — Ajout de `LSApplicationQueriesSchemes` (`itms-apps`) dans `Info.plist` pour permettre l'ouverture de l'App Store depuis l'app ; nettoyage d'un TODO résolu dans `useAppUpdateCheck.ts`

### Tableau de statistiques (`6888d83`)
- `6888d83` — Légende des stats rendue scrollable (`StatsLegendModal`) ; largeur du nom joueur fixée pour éviter le désalignement des colonnes (`StatsTab`) ; nettoyage mineur (`MatchDetailsScreen`)

### Bump
- `61ea0a4` — Bump to v1.1.0
- *(à ajouter au commit de bump vers v1.1.1)*
